# Preflight — organiser's §2.4 pre-submission checklist

**Result: 7/8 checks passed.**

- **Gateway:** `cctv.corp8.cloud`
- **Catalogue:** `https://cctv.corp8.cloud/cameras.json`
- **Transport:** RTSP over TCP
- **Commit:** `4934b2962eb67f6ea5b883eea2a22cbbc3f112b7` — **working tree dirty**, this artefact does not correspond to a commit
- **Branch:** `main`
- **Python:** 3.12.5 on Windows-11-10.0.26200-SP0

Every check marked *live* was exercised against the running gateway: a capture
opened, a feed interrupted, a hard scene cut fed to the detector. Checks marked
*static* are assertions over this repository's source. Nothing here reports a
pass on the strength of a code comment.

| # | Result | How | Check |
|---:|---|---|---|
| 1 | ✅ PASS | static+live | Every client forces RTSP over TCP (HLS fallback when 8554 is blocked) |
| 2 | ✅ PASS | static | No timing logic depends on declared FPS |
| 3 | ✅ PASS | live | Inter-frame gaps do not crash or stall the pipeline |
| 4 | ✅ PASS | live | Reconnect with backoff, tested by interrupting a feed |
| 5 | ✅ PASS | live | Decoder warnings on join are logged, not fatal |
| 6 | ✅ PASS | live | Camera list and properties are read from /api/ingest |
| 7 | ❌ FAIL | live | Pipeline handles mixed H.264/H.265 and mixed resolutions |
| 8 | ✅ PASS | live | Behaviour is sane across a scene discontinuity |

## Measured values behind each check

### 1. Every client forces RTSP over TCP (HLS fallback when 8554 is blocked)

**PASS** (static+live)

rtsp_transport forced to 'tcp' in every capture (options='rtsp_transport;tcp|timeout;15000000|max_delay;500000|reorder_queue_size;0|rw_timeout;20000000|reconnect;1|reconnect_streamed;1|live_start_index;-1'); RTSP:8554 reachable, used directly; 5 frames decoded over RTSP from camera cam01 (join 0.11s); cv2 imported only via services.common.cv_env

### 2. No timing logic depends on declared FPS

**PASS** (static)

2 reference-only use(s), all marked on the line (services\common\stream_client.py:200, services\ingest\file_source.py:111); 0 timing use(s). Expected count 2 — matches

### 3. Inter-frame gaps do not crash or stall the pipeline

**PASS** (live)

147 intervals on camera cam01: median 80ms, max 84ms (1.0x median), 48 frames decoded after the largest gap

### 4. Reconnect with backoff, tested by interrupting a feed

**PASS** (live)

28 frames, disconnect forced mid-stream, 1 reconnect(s), 31 frames after recovery (backoff envelope 2.0s-30.0s with full jitter, observed resume 0.0s); transport=rtsp

### 5. Decoder warnings on join are logged, not fatal

**PASS** (live)

camera cam01 (codec undeclared): 0 decode warning(s) absorbed during join, first frame at 0.12s, 25 frames decoded -- session never aborted

### 6. Camera list and properties are read from /api/ingest

**PASS** (live)

30 cameras from https://cctv.corp8.cloud/cameras.json; 0 declare codec+resolution, 30 report empty/zero properties and are resolved by probing (ids ['cam01', 'cam02', 'cam03', 'cam04', 'cam05', 'cam06', 'cam07', 'cam08']...)

### 7. Pipeline handles mixed H.264/H.265 and mixed resolutions

**FAIL** (live)

NOT EXERCISED: the catalogue does not declare both an H.264 and an H.265 camera

### 8. Behaviour is sane across a scene discontinuity

**PASS** (live)

40 consecutive frames from camera cam01: 0 false cut(s); hard cut to camera cam03 detected (hist_corr=0.43, mad=49.0); on detection trackers reset and a new session id is issued while written evidence is preserved

